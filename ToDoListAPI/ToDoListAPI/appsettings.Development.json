{
    "Logging": { // Configurazione dei livelli di logging:
        "LogLevel": {
            "Default": "Information", // - "Default" imposta il livello di log generale su "Information" (info generiche).
            "Microsoft.AspNetCore": "Warning" // - "Microsoft.AspNetCore" imposta i log di ASP.NET Core su livello "Warning" (solo avvisi e errori).
        }
    }
}
